package edu.gonzaga.renderer;

public interface SettingsPanelCallbacks {
    
    /*
     * User closes the settings panel, taking them back to the panel they were on previously.
     */
    void previousPanelOnCLoseSettings();
}
